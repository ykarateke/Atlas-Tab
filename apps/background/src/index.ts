// MV3 service worker entry — install/uninstall analytics events and alarms land here.
// The worker can be killed and restarted at any time; no in-memory state may be relied on
// to survive between events (ARCHITECTURE.md § 6).
export {};
